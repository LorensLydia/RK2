name: Build  CMake 

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: build
      run: |
        cmake -H. -B_build
        cmake --build _build
